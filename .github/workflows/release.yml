name: Create new github release

on:
  workflow_dispatch:
    inputs:
      version:
        required: true

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        ssh-key: ${{secrets.SSH_PRIVATE}}
    - run: git config --global user.email "tvpozytyw@gmail.com"
    - run: git config --global user.name "github-action"
    - name: Set up Go
      uses: actions/setup-go@v4
    - name: Build
      run: make build
    - name: Test
      run: make test
    - name: Prepare release
      env:
        tag: ${{ github.event.inputs.version }}
      run: make prepare-release version=$tag
    - run: echo "commit_sha=$(git rev-parse --verify HEAD)" >> $GITHUB_OUTPUT
      id: git
    - name: Create tag
      uses: actions/github-script@v5
      with:
        script: |
          github.rest.git.createRef({
            owner: context.repo.owner,
            repo: context.repo.repo,
            ref: 'refs/tags/${{ github.event.inputs.version }}',
            sha: '${{ steps.git.outputs.commit_sha }}'
          })
    - name: Release pushed tag
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        tag: ${{ github.event.inputs.version }}
      run: |
        gh release create "$tag" \
            --repo="$GITHUB_REPOSITORY" \
            --title="${tag}" \
            --generate-notes
    - name: After release
      env:
        tag: ${{ github.event.inputs.version }}
      run: make after-release

  build-linux:
    runs-on: ubuntu-latest
    needs: create-release
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ github.event.inputs.version }}
    - name: Set up Go
      uses: actions/setup-go@v4
    - name: Build release
      env:
        tag: ${{ github.event.inputs.version }}
      run: make build
    - name: Add executables to releae
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        tag: ${{ github.event.inputs.version }}
      run: |
        gh release upload "$tag" ngw \
            --repo="$GITHUB_REPOSITORY"

  build-windows:
    runs-on: ubuntu-latest
    needs: create-release
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ github.event.inputs.version }}
    - name: Set up Go
      uses: actions/setup-go@v4
    - name: Build release
      env:
        tag: ${{ github.event.inputs.version }}
        GOOS: windows
        GOARCH: amd64
      run: make build
    - name: Add executables to releae
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        tag: ${{ github.event.inputs.version }}
      run: |
        ls
        gh release upload "$tag" ngw.exe \
            --repo="$GITHUB_REPOSITORY"

